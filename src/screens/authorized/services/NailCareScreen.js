import React, { memo } from 'react';
import { View  } from 'react-native';
import { ITEM_WIDTH, ITEM_HEIGHT } from './../../../actions/types';

const NailCareScreen = () => {
  return(
    <></>
  )
}

export default memo(NailCareScreen);