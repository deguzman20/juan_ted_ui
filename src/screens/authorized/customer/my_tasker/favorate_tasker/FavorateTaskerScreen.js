import React, { memo } from 'react';
import { Text } from 'react-native';

const FavorateTaskerScreen = () => {
  return(
    <Text>Favorate Tasker</Text>
  )
}

export default memo(FavorateTaskerScreen);