import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  empty_conversation_txt: {
    textAlign: 'center',
    color: 'black'
  },
  empty_conversation_container: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1
  }
});