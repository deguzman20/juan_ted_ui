import React, { memo } from 'react';
import { View, Text } from 'react-native';

const CustomerRequestScreen = () => {
  return(
    <View>
      <Text>Customer Request</Text>
    </View>
  )
}

export default memo(CustomerRequestScreen);