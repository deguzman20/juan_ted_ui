import React, { memo } from 'react';
import { View } from 'react-native';
import { ITEM_WIDTH, ITEM_HEIGHT } from './../../../actions/types';

const HomeRepairScreen = () => {
  return(
    <></>
  )
}

export default memo(HomeRepairScreen);